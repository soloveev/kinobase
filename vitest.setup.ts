import '@testing-library/jest-dom/vitest';
import { vi } from 'vitest';

// Тесты не зависят от того, как владелец заполнил `src/site.config.ts`: контракт
// проверяется на значениях свежего клона, а заполненный конфиг не должен красить
// прогон — иначе честный онбординг закрывал бы выкатку (`deploy.sh` гонит тесты).
// Тест, которому нужны другие значения, ставит свой `vi.mock` того же модуля.
vi.mock('@/site.config', () => ({
  siteConfig: {
    title: 'Кино База',
    url: 'http://localhost:3000',
    author: '',
    authorLink: null,
    description: 'Личная база фильмов: что посмотрел, чего жду, разбор творческих методов',
  },
}));
